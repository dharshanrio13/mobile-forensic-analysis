# Runtime dependencies (based on actual imports in app/).
fastapi
uvicorn[standard]
pydantic
python-multipart

# Test dependencies (backend/tests/test_full_integration.py, test_api.py).
pytest
requests

# Intentionally absent: sqlalchemy, alembic and any database driver.
# This project has no database - state lives in app/state.py.
